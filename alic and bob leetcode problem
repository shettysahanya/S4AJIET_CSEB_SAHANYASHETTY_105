class Solution {
    public long flowerGame(int n, int m) {

        int count=0;
        for(int x=1;x<=m;x++){
            for(int y=1;y<=n;y++){
                if((x+y)%2==1)
                    count++;
            }
        }
       return(count);
    }
}
